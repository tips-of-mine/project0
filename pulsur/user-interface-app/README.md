# User Interface App

This application handles all user-facing interfaces, dashboards, data visualization, and Identity and Access Management (IAM) for client accounts within the Pulsur platform.

## URL

The primary access point for this application is: [https://interface.pulsur.com](https://interface.pulsur.com)

## Database Schema

Database schema to be defined based on MVP visualization and IAM features.

*(Details will be added here as the specific tables and structures for the user interface functionalities, data visualization requirements, and IAM components are finalized for the Minimum Viable Product.)*

## Development Notes

A `.gitignore` file should be added to exclude `node_modules/`, `dist/` build outputs, `.env` files (if used for environment-specific variables), `*.log` files, and potentially `config.json` if it is to contain sensitive production credentials. For this initial setup, `config.json` will be included in `.gitignore`.
